/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Simple config with no experimental flags that could cause issues
};

module.exports = nextConfig; 